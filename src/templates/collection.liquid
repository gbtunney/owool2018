
<!-- spurit_po-added -->
{% include 'spurit_po-collection-snippet' %}
<!-- /spurit_po-added -->
<div id="col-main" class="full {{ collection.handle }}">

  <section class="content-collection">
    <header class="content-header ">
     {% include 'breadcrumbs' %}
    </header>

    {% paginate collection.products by 21 %}
      {% if collection.products.size > 0 %}
    
        <ul class="product-grid a-product-grid ">
        {% for product in collection.products %}
          <li>
            {% include 'product-grid-item' %}
          </li>
        {% endfor %}
        </ul>

        {% include 'pagination' %}
      
      {% else %}    
        <strong>No products found in this collection.</strong>
      {% endif %}
    {% endpaginate %}
  </section>
  
</div> <!-- /#col-main -->